import { createTheme } from "@mui/material";

const MyTheme = createTheme({
    palette: {
      primary: {
        main: '#000000',
      },
      secondary: {
        main: '#000000',
      },
    },

    typography:{
        icon:{

        }
    }
  });


export default MyTheme;